# 智慧宠物管理系统 - 部署上线指南

## 📋 目录

1. [服务器配置要求](#服务器配置要求)
2. [环境准备](#环境准备)
3. [数据库部署](#数据库部署)
4. [后端部署](#后端部署)
5. [前端部署](#前端部署)
6. [Nginx配置](#nginx配置)
7. [上线验证](#上线验证)
8. [监控和维护](#监控和维护)

---

## 一、服务器配置要求

### 1.1 最低配置（开发/测试环境）
- **CPU**: 2核
- **内存**: 4GB
- **硬盘**: 50GB
- **带宽**: 5Mbps
- **操作系统**: CentOS 7+ / Ubuntu 18.04+

### 1.2 推荐配置（生产环境）
- **CPU**: 4核
- **内存**: 8GB
- **硬盘**: 100GB SSD
- **带宽**: 10Mbps
- **操作系统**: CentOS 7+ / Ubuntu 20.04+

### 1.3 高性能配置（大规模部署）
- **CPU**: 8核+
- **内存**: 16GB+
- **硬盘**: 200GB+ SSD
- **带宽**: 20Mbps+
- **负载均衡**: 支持
- **数据库**: 主从复制

---

## 二、环境准备

### 2.1 安装JDK 17

#### CentOS/RHEL
```bash
# 下载JDK
wget https://download.oracle.com/java/17/latest/jdk-17_linux-x64_bin.tar.gz

# 解压
tar -zxvf jdk-17_linux-x64_bin.tar.gz
sudo mv jdk-17 /usr/local/

# 配置环境变量
sudo vim /etc/profile
```

添加以下内容：
```bash
export JAVA_HOME=/usr/local/jdk-17
export PATH=$JAVA_HOME/bin:$PATH
export CLASSPATH=.:$JAVA_HOME/lib
```

```bash
# 使配置生效
source /etc/profile

# 验证安装
java -version
```

### 2.2 安装MySQL 8.0

#### CentOS/RHEL
```bash
# 添加MySQL仓库
sudo rpm -Uvh https://dev.mysql.com/get/mysql80-community-release-el7-3.noarch.rpm

# 安装MySQL
sudo yum install mysql-community-server -y

# 启动MySQL
sudo systemctl start mysqld
sudo systemctl enable mysqld

# 获取临时密码
sudo grep 'temporary password' /var/log/mysqld.log

# 修改root密码
mysql -u root -p
ALTER USER 'root'@'localhost' IDENTIFIED BY 'YourStrongPassword123!';
FLUSH PRIVILEGES;
```

### 2.3 安装Node.js 18+

```bash
# 使用NodeSource仓库
curl -fsSL https://rpm.nodesource.com/setup_18.x | sudo bash -
sudo yum install nodejs -y

# 验证安装
node -v
npm -v
```

### 2.4 安装Nginx

```bash
# 安装Nginx
sudo yum install nginx -y

# 启动Nginx
sudo systemctl start nginx
sudo systemctl enable nginx

# 验证安装
nginx -v
```

### 2.5 安装Redis（可选）

```bash
# 安装Redis
sudo yum install redis -y

# 启动Redis
sudo systemctl start redis
sudo systemctl enable redis

# 验证安装
redis-cli ping
```

---

## 三、数据库部署

### 3.1 创建数据库用户

```sql
-- 登录MySQL
mysql -u root -p

-- 创建数据库用户
CREATE USER 'petadmin'@'localhost' IDENTIFIED BY 'PetAdmin123!';
CREATE USER 'petadmin'@'%' IDENTIFIED BY 'PetAdmin123!';

-- 授予权限
GRANT ALL PRIVILEGES ON smart_pet_system.* TO 'petadmin'@'localhost';
GRANT ALL PRIVILEGES ON smart_pet_system.* TO 'petadmin'@'%';
FLUSH PRIVILEGES;
```

### 3.2 导入数据库

```bash
# 上传SQL文件到服务器
scp database/smart_pet_system.sql user@server:/tmp/

# 在服务器上执行
mysql -u petadmin -p < /tmp/smart_pet_system.sql

# 导入示例数据（可选）
mysql -u petadmin -p smart_pet_system < /tmp/insert_sample_data.sql

# 执行优化脚本
mysql -u petadmin -p smart_pet_system < /tmp/optimize_database.sql
```

### 3.3 数据库配置优化

编辑 `/etc/my.cnf`:
```ini
[mysqld]
# 基础配置
port = 3306
datadir = /var/lib/mysql
socket = /var/lib/mysql/mysql.sock

# 字符集
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci

# 连接数
max_connections = 200
max_connect_errors = 1000

# 缓冲池（根据服务器内存调整）
innodb_buffer_pool_size = 2G
innodb_log_file_size = 256M

# 查询缓存
query_cache_size = 64M
query_cache_type = 1

# 慢查询日志
slow_query_log = 1
slow_query_log_file = /var/log/mysql/slow.log
long_query_time = 2

# 二进制日志
log_bin = /var/log/mysql/mysql-bin.log
expire_logs_days = 7
```

重启MySQL:
```bash
sudo systemctl restart mysqld
```

---

## 四、后端部署

### 4.1 打包应用

在本地开发环境：
```bash
cd backend
mvn clean package -DskipTests
```

生成的jar包位于：`backend/target/smart-pet-system-1.0.0.jar`

### 4.2 上传到服务器

```bash
# 创建应用目录
ssh user@server "mkdir -p /opt/smart-pet-system"

# 上传jar包
scp backend/target/smart-pet-system-1.0.0.jar user@server:/opt/smart-pet-system/

# 上传配置文件
scp backend/src/main/resources/application-prod.yml user@server:/opt/smart-pet-system/
```

### 4.3 生产环境配置

创建 `application-prod.yml`:
```yaml
server:
  port: 8080
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smart_pet_system?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=Asia/Shanghai
    username: petadmin
    password: PetAdmin123!
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  redis:
    host: localhost
    port: 6379
    password: 
    database: 0
    timeout: 3000
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: -1ms

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.nolog.NoLoggingImpl
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

jwt:
  secret: your-production-secret-key-change-this-in-production
  expiration: 86400000

logging:
  level:
    root: INFO
    com.pet: INFO
  file:
    name: /var/log/smart-pet-system/application.log
  pattern:
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
```

### 4.4 创建启动脚本

创建 `/opt/smart-pet-system/start.sh`:
```bash
#!/bin/bash

APP_NAME=smart-pet-system
APP_JAR=smart-pet-system-1.0.0.jar
APP_HOME=/opt/smart-pet-system
LOG_DIR=/var/log/smart-pet-system
PID_FILE=$APP_HOME/$APP_NAME.pid

# 创建日志目录
mkdir -p $LOG_DIR

# 检查是否已运行
if [ -f "$PID_FILE" ]; then
    PID=$(cat $PID_FILE)
    if ps -p $PID > /dev/null 2>&1; then
        echo "$APP_NAME is already running (PID: $PID)"
        exit 1
    fi
fi

# 启动应用
echo "Starting $APP_NAME..."
nohup java -jar \
    -Xms1g -Xmx2g \
    -XX:+UseG1GC \
    -XX:MaxGCPauseMillis=200 \
    -Dspring.profiles.active=prod \
    $APP_HOME/$APP_JAR \
    > $LOG_DIR/console.log 2>&1 &

# 保存PID
echo $! > $PID_FILE
echo "$APP_NAME started (PID: $!)"
```

创建 `/opt/smart-pet-system/stop.sh`:
```bash
#!/bin/bash

APP_NAME=smart-pet-system
APP_HOME=/opt/smart-pet-system
PID_FILE=$APP_HOME/$APP_NAME.pid

if [ ! -f "$PID_FILE" ]; then
    echo "$APP_NAME is not running"
    exit 1
fi

PID=$(cat $PID_FILE)
if ! ps -p $PID > /dev/null 2>&1; then
    echo "$APP_NAME is not running"
    rm -f $PID_FILE
    exit 1
fi

echo "Stopping $APP_NAME (PID: $PID)..."
kill $PID

# 等待进程结束
for i in {1..30}; do
    if ! ps -p $PID > /dev/null 2>&1; then
        echo "$APP_NAME stopped"
        rm -f $PID_FILE
        exit 0
    fi
    sleep 1
done

# 强制结束
echo "Force stopping $APP_NAME..."
kill -9 $PID
rm -f $PID_FILE
echo "$APP_NAME stopped"
```

设置执行权限：
```bash
chmod +x /opt/smart-pet-system/start.sh
chmod +x /opt/smart-pet-system/stop.sh
```

### 4.5 创建系统服务

创建 `/etc/systemd/system/smart-pet-system.service`:
```ini
[Unit]
Description=Smart Pet Management System
After=network.target mysql.service

[Service]
Type=forking
User=root
WorkingDirectory=/opt/smart-pet-system
ExecStart=/opt/smart-pet-system/start.sh
ExecStop=/opt/smart-pet-system/stop.sh
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
```

启用服务：
```bash
sudo systemctl daemon-reload
sudo systemctl enable smart-pet-system
sudo systemctl start smart-pet-system
sudo systemctl status smart-pet-system
```

---

## 五、前端部署

### 5.1 构建前端

在本地开发环境：
```bash
cd frontend

# 安装依赖
npm install

# 构建生产版本
npm run build
```

生成的文件位于：`frontend/dist/`

### 5.2 上传到服务器

```bash
# 创建前端目录
ssh user@server "mkdir -p /var/www/smart-pet-system"

# 上传构建文件
scp -r frontend/dist/* user@server:/var/www/smart-pet-system/
```

### 5.3 配置环境变量

创建 `frontend/.env.production`:
```env
# API地址
VITE_API_BASE_URL=https://your-domain.com/api

# 应用标题
VITE_APP_TITLE=智慧宠物管理系统

# 上传文件大小限制（MB）
VITE_UPLOAD_SIZE=10
```

---

## 六、Nginx配置

### 6.1 创建Nginx配置

创建 `/etc/nginx/conf.d/smart-pet-system.conf`:
```nginx
# HTTP重定向到HTTPS
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;
    return 301 https://$server_name$request_uri;
}

# HTTPS配置
server {
    listen 443 ssl http2;
    server_name your-domain.com www.your-domain.com;

    # SSL证书配置
    ssl_certificate /etc/nginx/ssl/your-domain.crt;
    ssl_certificate_key /etc/nginx/ssl/your-domain.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # 日志配置
    access_log /var/log/nginx/smart-pet-system-access.log;
    error_log /var/log/nginx/smart-pet-system-error.log;

    # Gzip压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript 
               application/json application/javascript application/xml+rss;

    # 前端静态文件
    location / {
        root /var/www/smart-pet-system;
        index index.html;
        try_files $uri $uri/ /index.html;
        
        # 缓存配置
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # API代理
    location /api/ {
        proxy_pass http://localhost:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 超时配置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        
        # 缓冲配置
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
    }

    # 文件上传大小限制
    client_max_body_size 10M;

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}
```

### 6.2 测试和重启Nginx

```bash
# 测试配置
sudo nginx -t

# 重启Nginx
sudo systemctl restart nginx
```

---

## 七、上线验证

### 7.1 健康检查

创建健康检查脚本 `health-check.sh`:
```bash
#!/bin/bash

API_URL="https://your-domain.com/api"
FRONTEND_URL="https://your-domain.com"

echo "=== 健康检查开始 ==="

# 检查前端
echo "检查前端..."
FRONTEND_STATUS=$(curl -s -o /dev/null -w "%{http_code}" $FRONTEND_URL)
if [ "$FRONTEND_STATUS" = "200" ]; then
    echo "✓ 前端正常 (HTTP $FRONTEND_STATUS)"
else
    echo "✗ 前端异常 (HTTP $FRONTEND_STATUS)"
fi

# 检查后端
echo "检查后端..."
BACKEND_STATUS=$(curl -s -o /dev/null -w "%{http_code}" $API_URL/health)
if [ "$BACKEND_STATUS" = "200" ]; then
    echo "✓ 后端正常 (HTTP $BACKEND_STATUS)"
else
    echo "✗ 后端异常 (HTTP $BACKEND_STATUS)"
fi

# 检查数据库
echo "检查数据库..."
mysql -u petadmin -pPetAdmin123! -e "SELECT 1" smart_pet_system > /dev/null 2>&1
if [ $? -eq 0 ]; then
    echo "✓ 数据库正常"
else
    echo "✗ 数据库异常"
fi

# 检查Redis
echo "检查Redis..."
redis-cli ping > /dev/null 2>&1
if [ $? -eq 0 ]; then
    echo "✓ Redis正常"
else
    echo "✗ Redis异常"
fi

echo "=== 健康检查完成 ==="
```

### 7.2 功能验证清单

#### 用户模块
- [ ] 用户注册
- [ ] 用户登录
- [ ] 获取用户信息
- [ ] 修改用户信息
- [ ] 修改密码

#### 宠物模块
- [ ] 添加宠物
- [ ] 查看宠物列表
- [ ] 编辑宠物信息
- [ ] 删除宠物

#### 健康模块
- [ ] 添加健康记录
- [ ] 查看健康记录
- [ ] 健康趋势图表

#### 服务预约模块
- [ ] 查看服务列表
- [ ] 创建预约
- [ ] 查看预约列表
- [ ] 取消预约
- [ ] 评价服务

### 7.3 性能验证

```bash
# 使用ab进行压力测试
ab -n 1000 -c 100 https://your-domain.com/api/service/list

# 使用curl测试响应时间
time curl https://your-domain.com/api/user/info \
    -H "Authorization: Bearer YOUR_TOKEN"
```

### 7.4 安全验证

- [ ] HTTPS证书有效
- [ ] SQL注入防护
- [ ] XSS防护
- [ ] CSRF防护
- [ ] 密码加密
- [ ] JWT Token验证

---

## 八、监控和维护

### 8.1 日志管理

#### 应用日志
```bash
# 查看应用日志
tail -f /var/log/smart-pet-system/application.log

# 查看错误日志
grep ERROR /var/log/smart-pet-system/application.log

# 日志轮转配置
sudo vim /etc/logrotate.d/smart-pet-system
```

添加以下内容：
```
/var/log/smart-pet-system/*.log {
    daily
    rotate 30
    compress
    delaycompress
    notifempty
    create 0644 root root
    sharedscripts
    postrotate
        systemctl reload smart-pet-system > /dev/null 2>&1 || true
    endscript
}
```

#### Nginx日志
```bash
# 访问日志
tail -f /var/log/nginx/smart-pet-system-access.log

# 错误日志
tail -f /var/log/nginx/smart-pet-system-error.log
```

### 8.2 数据库备份

创建备份脚本 `/opt/backup/backup-database.sh`:
```bash
#!/bin/bash

BACKUP_DIR=/opt/backup/mysql
DATE=$(date +%Y%m%d_%H%M%S)
DB_NAME=smart_pet_system
DB_USER=petadmin
DB_PASS=PetAdmin123!

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份数据库
mysqldump -u$DB_USER -p$DB_PASS $DB_NAME | gzip > $BACKUP_DIR/${DB_NAME}_${DATE}.sql.gz

# 删除30天前的备份
find $BACKUP_DIR -name "*.sql.gz" -mtime +30 -delete

echo "Database backup completed: ${DB_NAME}_${DATE}.sql.gz"
```

设置定时任务：
```bash
# 编辑crontab
crontab -e

# 添加每天凌晨2点备份
0 2 * * * /opt/backup/backup-database.sh
```

### 8.3 监控告警

#### 系统监控
```bash
# 安装监控工具
sudo yum install sysstat -y

# 查看CPU使用率
mpstat 1 5

# 查看内存使用
free -h

# 查看磁盘使用
df -h

# 查看网络连接
netstat -tunlp | grep 8080
```

#### 应用监控
创建监控脚本 `/opt/monitor/check-app.sh`:
```bash
#!/bin/bash

# 检查应用是否运行
if ! systemctl is-active --quiet smart-pet-system; then
    echo "Application is down! Restarting..."
    systemctl start smart-pet-system
    # 发送告警邮件或短信
fi

# 检查内存使用
MEM_USAGE=$(ps aux | grep smart-pet-system | awk '{sum+=$4} END {print sum}')
if (( $(echo "$MEM_USAGE > 80" | bc -l) )); then
    echo "High memory usage: ${MEM_USAGE}%"
    # 发送告警
fi
```

设置定时检查：
```bash
# 每5分钟检查一次
*/5 * * * * /opt/monitor/check-app.sh
```

---

## 九、故障排查

### 9.1 常见问题

#### 问题1: 应用无法启动
```bash
# 检查日志
tail -100 /var/log/smart-pet-system/application.log

# 检查端口占用
netstat -tunlp | grep 8080

# 检查Java进程
ps aux | grep java
```

#### 问题2: 数据库连接失败
```bash
# 测试数据库连接
mysql -u petadmin -p -h localhost smart_pet_system

# 检查MySQL状态
systemctl status mysqld

# 查看MySQL错误日志
tail -100 /var/log/mysqld.log
```

#### 问题3: Nginx 502错误
```bash
# 检查后端是否运行
systemctl status smart-pet-system

# 检查Nginx配置
nginx -t

# 查看Nginx错误日志
tail -100 /var/log/nginx/smart-pet-system-error.log
```

### 9.2 性能优化

#### JVM调优
```bash
# 修改启动脚本，调整JVM参数
-Xms2g -Xmx4g \
-XX:+UseG1GC \
-XX:MaxGCPauseMillis=200 \
-XX:+HeapDumpOnOutOfMemoryError \
-XX:HeapDumpPath=/var/log/smart-pet-system/heapdump.hprof
```

#### 数据库优化
```sql
-- 查看慢查询
SELECT * FROM mysql.slow_log ORDER BY query_time DESC LIMIT 10;

-- 分析表
ANALYZE TABLE user, pet, health_record, appointment;

-- 优化表
OPTIMIZE TABLE user, pet, health_record, appointment;
```

---

## 十、回滚方案

### 10.1 应用回滚

```bash
# 停止当前版本
systemctl stop smart-pet-system

# 恢复旧版本
cp /opt/backup/smart-pet-system-1.0.0.jar.bak \
   /opt/smart-pet-system/smart-pet-system-1.0.0.jar

# 启动应用
systemctl start smart-pet-system
```

### 10.2 数据库回滚

```bash
# 恢复数据库备份
gunzip < /opt/backup/mysql/smart_pet_system_20251209_020000.sql.gz | \
    mysql -u petadmin -p smart_pet_system
```

---

## 附录

### A. 端口清单

| 服务 | 端口 | 说明 |
|------|------|------|
| Nginx | 80 | HTTP |
| Nginx | 443 | HTTPS |
| Spring Boot | 8080 | 后端API |
| MySQL | 3306 | 数据库 |
| Redis | 6379 | 缓存 |

### B. 目录结构

```
/opt/smart-pet-system/          # 应用目录
├── smart-pet-system-1.0.0.jar  # 应用jar包
├── application-prod.yml         # 生产配置
├── start.sh                     # 启动脚本
└── stop.sh                      # 停止脚本

/var/www/smart-pet-system/      # 前端目录
├── index.html
├── assets/
└── ...

/var/log/smart-pet-system/      # 日志目录
├── application.log
└── console.log

/opt/backup/                     # 备份目录
├── mysql/
└── app/
```

### C. 相关命令

```bash
# 启动服务
systemctl start smart-pet-system

# 停止服务
systemctl stop smart-pet-system

# 重启服务
systemctl restart smart-pet-system

# 查看状态
systemctl status smart-pet-system

# 查看日志
journalctl -u smart-pet-system -f

# 重新加载Nginx
nginx -s reload

# 测试Nginx配置
nginx -t
```

---

**文档维护**: 运维团队  
**最后更新**: 2025-12-09  
**版本**: v1.0.0
